import{Divider,SectionHeader,Timeline}from"@/components/design-system";import{getTimeline}from"@/features/timeline/query";export default async function Page(){const rows=await getTimeline();return <div className="grid gap-8"><SectionHeader level={1} title="Memory Timeline" description="A unified chronological projection; it never owns business state."/><Divider/><Timeline label="Unified sales activity" entries={rows.map(x=>({...x,time:new Date(x.time).toLocaleString()}))}/></div>}
