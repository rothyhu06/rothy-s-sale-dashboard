import{Divider,SectionHeader}from"@/components/design-system";import{listCustomers}from"@/features/customers/queries";import{listOpportunities}from"@/features/opportunities/queries";import{TaskForm}from"@/features/tasks/components/task-form";export default async function Page(){const[customers,opps]=await Promise.all([listCustomers(),listOpportunities()]);return <div className="grid gap-8"><SectionHeader level={1} title="New Task" description="A concrete commitment, linked to the work it advances."/><Divider/><TaskForm customers={customers.map(c=>({id:c.id,name:c.name}))} opportunities={opps as never}/></div>}
