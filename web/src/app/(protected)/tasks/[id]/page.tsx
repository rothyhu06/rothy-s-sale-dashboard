import{Card,Divider,SectionHeader}from"@/components/design-system";import{TaskStatusForm}from"@/features/tasks/components/task-status-form";import{getTask}from"@/features/tasks/queries";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const t=await getTask(id);return <div className="grid gap-8"><SectionHeader level={1} title={t.title} description={t.status}/><Card variant="entity"><p>{t.description||"No description"}</p><p className="type-body-sm text-muted">{t.due_at?`Due ${new Date(t.due_at).toLocaleString()}`:"No due date"}</p></Card><Divider/><TaskStatusForm id={id} status={t.status} version={t.version}/></div>}
