/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from"next/link";import{Card,Divider,EmptyState,SectionHeader}from"@/components/design-system";import{listContacts}from"@/features/contacts/queries";
export default async function ContactsPage(){const rows=await listContacts();return <div className="grid gap-8"><SectionHeader level={1} title="Contacts" description="People stay stable; their opportunity roles remain contextual."/><Divider/>{rows.length?<div className="grid gap-4 sm:grid-cols-2">{rows.map((r:any)=><Card href={`/contacts/${r.id}`} key={r.id} variant="entity"><h2 className="type-heading-3">{r.full_name}</h2><p className="type-body-sm text-muted">{[r.department,r.position].filter(Boolean).join(" · ")||"Contact"}</p></Card>)}</div>:<EmptyState action={<Link className="text-accent" href="/customers">Open Customers</Link>} title="No contacts yet" description="Add contacts from a Customer workspace."/>}</div>}
