/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from"next/link";import{Card,Divider,EmptyState,SectionHeader}from"@/components/design-system";import{listInteractions}from"@/features/interactions/queries";export default async function Page(){const rows=await listInteractions();return <div className="grid gap-8"><SectionHeader level={1} title="Interactions" description="The factual center of customer meetings, calls and collaboration." action={<Link className="text-accent" href="/interactions/new">Record Interaction</Link>}/><Divider/>{rows.length?<div className="grid gap-4">{rows.map((x:any)=><Card href={`/interactions/${x.id}`} key={x.id} variant="entity"><p className="type-label text-accent">{x.interaction_type} · {x.outcome}</p><h2 className="type-heading-3">{x.title}</h2><p className="type-body-sm text-muted">{new Date(x.occurred_at).toLocaleString()}</p></Card>)}</div>:<EmptyState action={<Link className="text-accent" href="/interactions/new">Record Interaction</Link>} title="No interactions yet" description="Capture the first customer fact."/>}</div>}
