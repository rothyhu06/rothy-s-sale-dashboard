import{Divider,SectionHeader}from"@/components/design-system";import{listCustomers}from"@/features/customers/queries";import{OpportunityForm}from"@/features/opportunities/components/opportunity-form";
export default async function NewOpportunityPage({searchParams}:{searchParams:Promise<{customerId?:string}>}){const[{customerId},customers]=await Promise.all([searchParams,listCustomers()]);return <div className="grid gap-8"><SectionHeader level={1} title="New Opportunity" description="Start at Lead; stage and outcome history remain traceable."/><Divider/><OpportunityForm customerId={customerId} customers={customers.map(c=>({id:c.id,name:c.name}))}/></div>}
