/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from"next/link";import{Card,Divider,EmptyState,SectionHeader}from"@/components/design-system";import{listReports}from"@/features/reports/queries";export default async function Page(){const rows=await listReports();return <div className="grid gap-8"><SectionHeader level={1} title="Reports" description="Your interpretation of real work facts; never a duplicate source of truth." action={<Link className="text-accent" href="/reports/daily">Write Daily Report</Link>}/><Divider/>{rows.length?<div className="grid gap-4">{rows.map((x:any)=><Card key={x.id} variant="entity"><p className="type-label text-accent">{x.report_type}</p><h2 className="type-heading-3">{x.report_date||`${new Date(x.period_start).toLocaleDateString()} week`}</h2><p className="type-body-sm text-muted">{x.focus||x.reflection||"No narrative"}</p></Card>)}</div>:<EmptyState action={<Link className="text-accent" href="/reports/daily">Start today</Link>} title="No reports yet" description="Close the day with a short reflection."/>}</div>}
