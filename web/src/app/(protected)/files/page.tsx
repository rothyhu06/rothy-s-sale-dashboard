/* eslint-disable @typescript-eslint/no-explicit-any */
import{Badge,Card,Divider,SectionHeader}from"@/components/design-system";import{AttachmentForm,TagForm}from"@/features/files/components/file-manager-forms";import{downloadAttachment}from"@/features/files/page-actions";import{createServerClient}from"@/lib/supabase/server";export default async function Page(){const c=await createServerClient();const[a,t]=await Promise.all([c.from("attachments").select("id,original_filename,file_category,size_bytes,storage_status,data_level,version").order("created_at",{ascending:false}),c.from("tags").select("id,name,description,data_level").order("name")]);return <div className="grid gap-10"><SectionHeader level={1} title="Files & Tags" description="Private attachments and reusable classification."/><div className="grid gap-8 lg:grid-cols-2"><Card variant="entity"><h2 className="type-heading-2">Upload Attachment</h2><AttachmentForm/></Card><Card variant="entity"><h2 className="type-heading-2">Create Tag</h2><TagForm/></Card></div><Divider/><section><h2 className="type-heading-2">Private Attachments</h2><div className="grid gap-3">{(a.data??[]).map((x:any)=><Card key={x.id} variant="entity"><div className="flex flex-wrap items-center justify-between gap-4"><div><strong>{x.original_filename}</strong><p className="type-body-sm text-muted">{x.file_category} · {Math.ceil(x.size_bytes/1024)} KB · {x.data_level}</p></div><div className="flex items-center gap-3"><Badge tone={x.storage_status==="Available"?"success":"neutral"}>{x.storage_status}</Badge>{x.storage_status==="Available"?<form action={downloadAttachment}><input name="attachmentId" type="hidden" value={x.id}/><button className="type-control min-h-11 text-accent" type="submit">Download</button></form>:null}</div></div></Card>)}</div></section><section><h2 className="type-heading-2">Tags</h2><div className="flex flex-wrap gap-3">{(t.data??[]).map((x:any)=><Badge key={x.id} tone="accent">{x.name} · {x.data_level}</Badge>)}</div></section></div>}
