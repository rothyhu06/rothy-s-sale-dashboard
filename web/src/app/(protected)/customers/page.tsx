import Link from "next/link";import{Card,Divider,EmptyState,SectionHeader}from"@/components/design-system";import{listCustomers}from"@/features/customers/queries";
export default async function CustomersPage(){const rows=await listCustomers();return <div className="grid gap-8"><SectionHeader level={1} title="Customers" description="Stable institution facts and the starting point for every sales action." action={<Link className="type-control text-accent" href="/customers/new">New Customer</Link>}/><Divider/>{rows.length?<div className="grid gap-4 sm:grid-cols-2">{rows.map(r=><Card href={`/customers/${r.id}`} key={r.id} variant="entity"><p className="type-label text-muted">{r.customer_type}</p><h2 className="type-heading-3 mt-2">{r.name}</h2><p className="type-body-sm text-muted">{[r.education_segment,r.region].filter(Boolean).join(" · ")||"Institution profile"}</p></Card>)}</div>:<EmptyState title="No customers yet" description="Create the first institution profile." action={<Link className="text-accent" href="/customers/new">Create Customer</Link>}/>}</div>}
