import{Badge,Card,Divider,SectionHeader}from"@/components/design-system";import{InsightStatusForm}from"@/features/insights/components/insight-status-form";import{getInsight}from"@/features/insights/queries";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const x=await getInsight(id);return <div className="grid gap-8"><SectionHeader level={1} title={x.title} description={x.insight_type}/><Badge tone="accent">{x.status}</Badge><Divider/><Card variant="entity"><p className="type-heading-3">{x.summary}</p><p>{x.content}</p></Card><InsightStatusForm id={id} mastery={x.mastery} status={x.status} version={x.version}/><p className="type-body-sm text-muted">Evidence is preserved by real owner-aware foreign keys.</p></div>}
