/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from"next/link";import{Badge,Card,Divider,EmptyState,SectionHeader}from"@/components/design-system";import{listInsights}from"@/features/insights/queries";export default async function Page(){const rows=await listInsights();return <div className="grid gap-8"><SectionHeader level={1} title="Insights" description="Reasoned cognition grounded in real evidence—not another notes folder." action={<Link className="text-accent" href="/insights/new">New Insight</Link>}/><Divider/>{rows.length?<div className="grid gap-4 sm:grid-cols-2">{rows.map((x:any)=><Card href={`/insights/${x.id}`} key={x.id} variant="entity"><Badge tone="accent">{x.status}</Badge><h2 className="type-heading-3 mt-3">{x.title}</h2><p className="type-body-sm text-muted">{x.summary}</p></Card>)}</div>:<EmptyState action={<Link className="text-accent" href="/insights/new">Create Insight</Link>} title="No insights yet" description="Turn one fact into a reusable judgment."/>}</div>}
