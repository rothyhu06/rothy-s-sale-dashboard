import{Divider,SectionHeader}from"@/components/design-system";import{InsightForm}from"@/features/insights/components/insight-form";import{listInteractions}from"@/features/interactions/queries";import{listOpportunities}from"@/features/opportunities/queries";import{listTasks}from"@/features/tasks/queries";export default async function Page(){const[interactions,tasks,opps]=await Promise.all([listInteractions(),listTasks(),listOpportunities()]);return <div className="grid gap-8"><SectionHeader level={1} title="New Insight" description="Explain what the evidence means and why it matters."/><Divider/><InsightForm interactions={interactions as never} opportunities={opps as never} tasks={tasks as never}/></div>}
