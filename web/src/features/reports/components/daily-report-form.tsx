"use client";import{useActionState,useState}from"react";import{Button,InputField,TextArea,TextInput}from"@/components/design-system";import{submitDailyReport,type State}from"../page-actions";export function DailyReportForm(){const[state,action,pending]=useActionState<State,FormData>(submitDailyReport,{});const[id]=useState(()=>crypto.randomUUID());return <form action={action} className="grid gap-5"><input name="clientRequestId" type="hidden" value={id}/><InputField id="report-date" label="Date" required><TextInput defaultValue={new Date().toISOString().slice(0,10)} name="date" type="date" required/></InputField>{[["focus","Today’s focus"],["reflection","Reflection"],["risks","Problems and risks"],["nextPlan","Tomorrow’s plan"]].map(([n,l])=><InputField id={`report-${n}`} key={n} label={l}><TextArea name={n}/></InputField>)}{state.message?<p role="alert" className="text-danger">{state.message}</p>:null}<Button loading={pending} size="large" type="submit">Save Daily Report</Button></form>}
