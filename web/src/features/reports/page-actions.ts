"use server";import{redirect}from"next/navigation";import{saveDailyReport,saveWeeklyReport}from"./actions";import{safeActionError}from"@/lib/actions/safe-action-error";export type State={message?:string};const t=(d:FormData,n:string)=>String(d.get(n)??"").trim();export async function submitDailyReport(_:State,d:FormData):Promise<State>{try{await saveDailyReport({date:t(d,"date"),focus:t(d,"focus"),reflection:t(d,"reflection"),risks:t(d,"risks"),nextPlan:t(d,"nextPlan")},t(d,"clientRequestId"));redirect("/reports")}catch(e){if((e as{digest?:string}).digest?.startsWith("NEXT_REDIRECT"))throw e;return{message:safeActionError(e,{operation:"save-report",fallback:"日报未能保存。"})}}}export async function submitWeeklyReport(_:State,d:FormData):Promise<State>{try{const start=new Date(`${t(d,"weekStart")}T00:00:00+08:00`),end=new Date(start.getTime()+7*86400000);await saveWeeklyReport({periodStart:start.toISOString(),periodEnd:end.toISOString(),focus:t(d,"focus"),reflection:t(d,"reflection"),risks:t(d,"risks"),nextPlan:t(d,"nextPlan")},t(d,"clientRequestId"));redirect("/reports")}catch(e){if((e as{digest?:string}).digest?.startsWith("NEXT_REDIRECT"))throw e;return{message:safeActionError(e,{operation:"save-weekly-report",fallback:"周报未能保存。"})}}}
