import "server-only";import{z}from"zod";import{runServiceCommand}from"@/lib/commands/run-service-command";
export const InteractionInput=z.object({customerId:z.uuid(),contactId:z.uuid().nullable().optional(),opportunityId:z.uuid().nullable().optional(),interactionType:z.enum(["Meeting","Call","Message","Workshop","POC","Internal Collaboration","Other"]),occurredAt:z.iso.datetime(),title:z.string().trim().min(1).max(300),summary:z.string().nullable().optional(),notes:z.string().nullable().optional(),outcome:z.enum(["Positive","Neutral","Negative","Pending"]),salesImpact:z.string().nullable().optional(),followUpRequired:z.boolean().default(false)});
export async function createInteraction(input:z.input<typeof InteractionInput>,requestId:string){const v=InteractionInput.parse(input);return runServiceCommand("CreateInteraction","create_interaction",requestId,{p_customer_id:v.customerId,p_contact_id:v.contactId??null,p_opportunity_id:v.opportunityId??null,p_interaction_type:v.interactionType,p_occurred_at:v.occurredAt,p_title:v.title,p_summary:v.summary??null,p_notes:v.notes??null,p_outcome:v.outcome,p_sales_impact:v.salesImpact??null,p_follow_up_required:v.followUpRequired,p_participant_snapshot:[]})}
